import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.voltex.app',
  appName: 'voltex-frontend',
  webDir: 'dist/voltex-frontend/browser'
};

export default config;